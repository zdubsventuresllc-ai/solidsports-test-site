{
  "name": "solid-website",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "serve dist -l ${PORT:-3000}",
    "shopify:sync": "node scripts/setup-shopify.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "serve": "^14.2.0"
  }
}
